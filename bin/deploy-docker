#!/bin/bash

docker-compose down
docker-compose build
docker-compose run --rm webpack webpack
docker-compose run --rm django /opt/ikaro/ikarodjango/manage.py migrate
docker-compose run --rm django /opt/ikaro/ikarodjango/manage.py collectstatic --noinput
docker-compose up -d
